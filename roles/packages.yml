- name: Remove unnecessary packages
  apt:
    name:
      - rsh-client
      - rsh-redone-client
      - talk
      - xinetd
      - tftp
      - vsftpd
    state: absent
    purge: yes

- name: Autoremove unused packages
  apt:
    autoremove: yes
